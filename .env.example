# MongoDB Connection String (Optional - App falls back to interactive Mock Store if absent)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/bricks?retryWrites=true&w=majority